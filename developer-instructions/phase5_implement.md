cwd = sandbox/<project-name>/codebase/

Implement all the features strictly from PRD. 
- Make sure your code is adheres to the MANDATORY MODULAR ARCHITECTURE rules.
- Ensure the application runs without errors locally.
- Verify the app builds successfully (`npm run build`) before proceeding.
- When feature implementation is complete, proceed to **PHASE 5.5 — Unit Tests**.
