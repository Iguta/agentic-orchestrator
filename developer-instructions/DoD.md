You are DONE only when ALL are true:

- PHASES 0 → 7 completed
- All PRD requirements implemented
- UI/UX quality meets standards
- Unit tests written for all Must-Have features
- README.md exists at PROJECT ROOT
- TESTING.md exists at PROJECT ROOT
- UNIT-TEST-REPORT.md exists at PROJECT ROOT
- SMOKE-TEST-REPORT.md exists at PROJECT ROOT
- npm test passes (run from codebase/)
- npm run build passes (run from codebase/)

When ALL are true:
STOP immediately and produce FINAL OUTPUT.